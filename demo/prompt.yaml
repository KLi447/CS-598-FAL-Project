template: |
  ### Instruction:
  {{ instruction }}

  ### Input:
  {{ input_template.format(text=data_point[text_column]) }}

  ### Output:
  {{ output_template.format(label=data_point[label_column]) }}
